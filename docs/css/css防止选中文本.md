# css防止选中文本

### **user-select: none;**一般来说要么不用就默认，要么就用none

| auto | 默认。如果浏览器允许，则可以选择文本。 |
| ---- | -------------------------------------- |
| none | 防止文本选取。                         |
| text | 文本可被用户选取。                     |
| all  | 单击选取文本，而不是双击。             |